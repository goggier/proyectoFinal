import { SizeCabecerasDirective } from './size-cabeceras.directive';

describe('SizeCabecerasDirective', () => {
  it('should create an instance', () => {
    const directive = new SizeCabecerasDirective();
    expect(directive).toBeTruthy();
  });
});
